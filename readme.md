# Go realworld api

This project demonstrates a small go api with:
. users model
. organizations model
. rest endpoints
. postgres database
. jwt authorization
POST /signup
POST /login
Get /users
PUT /users
GET /api/orgs
GET /api/users
